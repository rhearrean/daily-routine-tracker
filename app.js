const HABITS_KEY="dailyRoutineHabits.v9";const COMPLETIONS_KEY="dailyRoutineCompletions.v9";const OLD_KEYS=[["dailyRoutineHabits.v8","dailyRoutineCompletions.v8"],["dailyRoutineHabits.v7","dailyRoutineCompletions.v7"],["dailyRoutineHabits.v6","dailyRoutineCompletions.v6"],["dailyRoutineHabits.v5","dailyRoutineCompletions.v5"],["dailyRoutineHabits.v4","dailyRoutineCompletions.v4"],["dailyRoutineHabits.v3","dailyRoutineCompletions.v3"],["dailyRoutineHabits.v2","dailyRoutineCompletions.v2"]];
const defaultHabits=[{id:"read-book",name:"Read my book",schedule:"daily",days:[],block:"evening",cutoff:"",allowLate:true}];
const BLOCKS=[{id:"early",label:"🌅 Early Morning"},{id:"morning",label:"☀️ Morning"},{id:"afternoon",label:"🌤 Afternoon"},{id:"evening",label:"🌙 Evening"},{id:"anytime",label:"Anytime"}];
const $=id=>document.getElementById(id);const todayTitle=$("todayTitle"),dateText=$("dateText"),progressText=$("progressText"),progressPercent=$("progressPercent"),timeBlocks=$("timeBlocks"),emptyTodayText=$("emptyTodayText"),resetTodayBtn=$("resetTodayBtn"),streakText=$("streakText"),weekText=$("weekText"),recentDays=$("recentDays"),habitForm=$("habitForm"),habitName=$("habitName"),habitSchedule=$("habitSchedule"),customDays=$("customDays"),allHabits=$("allHabits"),habitStats=$("habitStats"),openSettingsBtn=$("openSettingsBtn"),closeSettingsBtn=$("closeSettingsBtn"),settingsPanel=$("settingsPanel"),exportBtn=$("exportBtn"),importBtn=$("importBtn"),backupBox=$("backupBox"),backupMessage=$("backupMessage"),formTitle=$("formTitle"),formModeLabel=$("formModeLabel"),saveHabitBtn=$("saveHabitBtn"),cancelEditBtn=$("cancelEditBtn"),habitBlock=$("habitBlock"),habitCutoff=$("habitCutoff"),habitAllowLate=$("habitAllowLate");let selectedCustomDays=[],editingHabitId=null;
function safeParse(v,f){try{return JSON.parse(v)||f}catch{return f}}function getLocalDateKey(d=new Date()){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}function dateFromOffset(o=0){const d=new Date();d.setHours(12,0,0,0);d.setDate(d.getDate()+o);return d}function getTodayKey(){return getLocalDateKey(new Date())}function nowMinutes(){const d=new Date();return d.getHours()*60+d.getMinutes()}function timeToMinutes(v){if(!v)return null;const [h,m]=v.split(":").map(Number);return h*60+m}function formatTime(v){if(!v)return"No cutoff";const [h,m]=v.split(":").map(Number),d=new Date();d.setHours(h,m,0,0);return d.toLocaleTimeString(undefined,{hour:"numeric",minute:"2-digit"})}
function makeId(name){const slug=name.trim().toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"");return `${slug||"habit"}-${Date.now()}`}function normalizeHabit(h){return{id:h.id||makeId(h.name||"Habit"),name:h.name||"Habit",schedule:h.schedule||"daily",days:Array.isArray(h.days)?h.days:[],block:h.block||"anytime",cutoff:h.cutoff||"",allowLate:h.allowLate!==false}}
function migrateOldDataOnce(){if(!localStorage.getItem(HABITS_KEY)){for(const [hk]of OLD_KEYS){const oh=safeParse(localStorage.getItem(hk),null);if(Array.isArray(oh)&&oh.length>0){localStorage.setItem(HABITS_KEY,JSON.stringify(oh.map(normalizeHabit)));break}}}if(!localStorage.getItem(COMPLETIONS_KEY)){for(const[,ck]of OLD_KEYS){const oc=safeParse(localStorage.getItem(ck),null);if(oc&&typeof oc==="object"){localStorage.setItem(COMPLETIONS_KEY,JSON.stringify(oc));break}}}}
function loadHabits(){const s=safeParse(localStorage.getItem(HABITS_KEY),null);if(!s||!Array.isArray(s)||s.length===0){saveHabits(defaultHabits);return defaultHabits}return s.map(normalizeHabit)}function saveHabits(h){localStorage.setItem(HABITS_KEY,JSON.stringify(h.map(normalizeHabit)))}function loadCompletions(){return safeParse(localStorage.getItem(COMPLETIONS_KEY),{})}function saveCompletions(d){localStorage.setItem(COMPLETIONS_KEY,JSON.stringify(d))}
function formatDateLabel(){const d=new Date();todayTitle.textContent=d.toLocaleDateString(undefined,{weekday:"long"});dateText.textContent=d.toLocaleDateString(undefined,{month:"long",day:"numeric",year:"numeric"})}function isHabitDue(h,d){const day=d.getDay();if(h.schedule==="daily")return true;if(h.schedule==="weekdays")return day>=1&&day<=5;if(h.schedule==="weekends")return day===0||day===6;if(h.schedule==="custom")return h.days.includes(day);return true}function habitsDueOn(d){return loadHabits().filter(h=>isHabitDue(h,d))}function completionEntry(k,id){return loadCompletions()[k]?.[id]}function isDone(k,id){return Boolean(completionEntry(k,id))}function completionStatus(k,id){const e=completionEntry(k,id);return typeof e==="object"?e.status:(e?"done":"")}function isPastCutoff(h){const c=timeToMinutes(h.cutoff);return c!==null&&nowMinutes()>c}function getCurrentCompletionStatus(h){return h.cutoff&&isPastCutoff(h)?"late":"done"}
function toggleDone(k,h){const c=loadCompletions();c[k]=c[k]||{};if(c[k][h.id])delete c[k][h.id];else{const status=getCurrentCompletionStatus(h);if(status==="late"&&h.allowLate===false){alert("This habit is past its cutoff time.");return}c[k][h.id]={done:true,status,completedAt:new Date().toISOString()}}saveCompletions(c);render()}function resetToday(){const c=loadCompletions();delete c[getTodayKey()];saveCompletions(c);render()}function deleteHabit(id){saveHabits(loadHabits().filter(h=>h.id!==id));const c=loadCompletions();Object.keys(c).forEach(k=>{if(c[k])delete c[k][id]});saveCompletions(c);if(editingHabitId===id)resetFormMode();render()}
function getDailyProgress(d){const k=getLocalDateKey(d),due=habitsDueOn(d),total=due.length,completed=due.filter(h=>isDone(k,h.id)).length;return{completed,total,percent:total===0?100:Math.round(completed/total*100)}}function getOverallCurrentStreak(){const t=getDailyProgress(new Date());let o=t.total>0&&t.completed<t.total?-1:0,s=0;for(;o>-365;o--){const p=getDailyProgress(dateFromOffset(o));if(p.total===0)continue;if(p.completed===p.total)s++;else break}return s}function getHabitCurrentStreak(h){let o=isHabitDue(h,new Date())&&!isDone(getTodayKey(),h.id)?-1:0,s=0;for(;o>-365;o--){const d=dateFromOffset(o),k=getLocalDateKey(d);if(!isHabitDue(h,d))continue;if(isDone(k,h.id))s++;else break}return s}function getHabitLongestStreak(h,days=365){let best=0,cur=0;for(let o=-days+1;o<=0;o++){const d=dateFromOffset(o),k=getLocalDateKey(d);if(!isHabitDue(h,d))continue;if(isDone(k,h.id)){cur++;best=Math.max(best,cur)}else cur=0}return best}function getHabitWindowStats(h,days){let due=0,completed=0,onTime=0,late=0,missed=0;for(let o=-days+1;o<=0;o++){const d=dateFromOffset(o),k=getLocalDateKey(d);if(!isHabitDue(h,d))continue;due++;if(isDone(k,h.id)){completed++;completionStatus(k,h.id)==="late"?late++:onTime++}else missed++}return{completed,due,missed,onTime,late,percent:due===0?0:Math.round(completed/due*100),onTimePercent:completed===0?0:Math.round(onTime/completed*100)}}function getHabitTotalStats(h){const c=loadCompletions();let completed=0,onTime=0,late=0;Object.keys(c).forEach(k=>{if(c[k]?.[h.id]){completed++;completionStatus(k,h.id)==="late"?late++:onTime++}});return{completed,onTime,late}}function getWeekCount(){let completed=0,daysWithHabits=0;for(let o=0;o>-7;o--){const p=getDailyProgress(dateFromOffset(o));if(p.total===0)continue;daysWithHabits++;if(p.completed===p.total)completed++}return{completed,daysWithHabits}}function scheduleLabel(h){if(h.schedule==="daily")return"Every day";if(h.schedule==="weekdays")return"Weekdays";if(h.schedule==="weekends")return"Weekends";const labels=["Sun","Mon","Tue","Wed","Thu","Fri","Sat"];return h.days.map(d=>labels[d]).join(", ")||"Custom"}function blockLabel(id){return BLOCKS.find(b=>b.id===id)?.label||"Anytime"}function getHabitStats(h){const l7=getHabitWindowStats(h,7),l30=getHabitWindowStats(h,30),tot=getHabitTotalStats(h);return{id:h.id,name:h.name,schedule:scheduleLabel(h),block:blockLabel(h.block),cutoff:h.cutoff||"",allowLate:h.allowLate,currentStreak:getHabitCurrentStreak(h),longestStreak:getHabitLongestStreak(h),last7Days:l7,last30Days:l30,totalCompleted:tot.completed,totalOnTime:tot.onTime,totalLate:tot.late}}
function renderTimeBlocks(){timeBlocks.innerHTML="";const due=habitsDueOn(new Date()),key=getTodayKey();emptyTodayText.classList.toggle("hidden",due.length>0);BLOCKS.forEach(block=>{const habits=due.filter(h=>h.block===block.id);if(habits.length===0)return;const completed=habits.filter(h=>isDone(key,h.id)).length,wrap=document.createElement("div");wrap.className="time-block";wrap.innerHTML=`<div class="time-block-header"><h3>${block.label}</h3><span class="block-progress">${completed}/${habits.length}</span></div><div class="habit-list"></div>`;const list=wrap.querySelector(".habit-list");habits.forEach(h=>{const b=document.createElement("button"),done=isDone(key,h.id),status=completionStatus(key,h.id),locked=!done&&h.allowLate===false&&isPastCutoff(h);b.className=`habit-button ${done?(status==="late"?"late":"done"):""} ${locked?"locked":""}`;b.type="button";const pill=done?`<span class="status-pill ${status==="late"?"late":"on-time"}">${status==="late"?"Late":"On time"}</span>`:locked?`<span class="status-pill locked">Missed cutoff</span>`:h.cutoff?`<span class="status-pill">Target: ${formatTime(h.cutoff)}</span>`:"";b.innerHTML=`<span class="checkbox" aria-hidden="true"></span><span><strong>${h.name}</strong><small>${h.cutoff?`Target: ${formatTime(h.cutoff)}`:"No target time"}</small>${pill}</span>`;b.addEventListener("click",()=>toggleDone(key,h));list.appendChild(b)});timeBlocks.appendChild(wrap)})}
function renderRecentDays(){recentDays.innerHTML="";for(let o=-6;o<=0;o++){const d=dateFromOffset(o),p=getDailyProgress(d),el=document.createElement("div");el.className=`day-pill ${p.total>0&&p.completed===p.total?"done":""}`;el.textContent=d.toLocaleDateString(undefined,{weekday:"short"});recentDays.appendChild(el)}}function renderAllHabits(){allHabits.innerHTML="";const habits=loadHabits();if(habits.length===0){allHabits.innerHTML=`<p class="empty-text">No habits yet. Add one above.</p>`;return}habits.forEach(h=>{const row=document.createElement("div");row.className="habit-row";row.innerHTML=`<div><strong>${h.name}</strong><small>${blockLabel(h.block)} • ${scheduleLabel(h)} • ${formatTime(h.cutoff)}${h.allowLate?"":" • locks after cutoff"}</small></div><div class="habit-actions"><button class="edit-btn" type="button">Edit</button><button class="danger-btn" type="button">Delete</button></div>`;row.querySelector(".edit-btn").addEventListener("click",()=>startEditHabit(h.id));row.querySelector(".danger-btn").addEventListener("click",()=>{if(confirm(`Delete "${h.name}"?`))deleteHabit(h.id)});allHabits.appendChild(row)})}function renderHabitStats(){habitStats.innerHTML="";const habits=loadHabits();if(habits.length===0){habitStats.innerHTML=`<p class="empty-text">No habit stats yet.</p>`;return}habits.forEach(h=>{const s=getHabitStats(h),card=document.createElement("div");card.className="habit-stat-card";card.innerHTML=`<strong>${s.name}</strong><small>${s.block} • ${s.schedule} • ${formatTime(s.cutoff)}</small><div class="metric-row"><div class="metric"><b>${s.currentStreak}</b><span>Current</span></div><div class="metric"><b>${s.longestStreak}</b><span>Longest</span></div><div class="metric"><b>${s.last30Days.percent}%</b><span>30 Days</span></div><div class="metric"><b>${s.last30Days.onTimePercent}%</b><span>On Time</span></div><div class="metric"><b>${s.totalLate}</b><span>Late</span></div><div class="metric"><b>${s.totalCompleted}</b><span>Total</span></div></div>`;habitStats.appendChild(card)})}
function render(){const p=getDailyProgress(new Date());renderTimeBlocks();progressText.textContent=`${p.completed}/${p.total}`;progressPercent.textContent=`${p.percent}%`;document.documentElement.style.setProperty("--progress",p.percent);const streak=getOverallCurrentStreak();streakText.textContent=`${streak} ${streak===1?"day":"days"}`;const week=getWeekCount();weekText.textContent=`${week.completed}/${week.daysWithHabits||7}`;renderRecentDays();renderAllHabits();renderHabitStats()}
function setSelectedCustomDays(days){selectedCustomDays=[...days];customDays.querySelectorAll("button").forEach(b=>b.classList.toggle("selected",selectedCustomDays.includes(Number(b.dataset.day))))}function resetFormMode(){editingHabitId=null;habitName.value="";habitSchedule.value="daily";habitBlock.value="evening";habitCutoff.value="";habitAllowLate.checked=true;setSelectedCustomDays([]);customDays.classList.add("hidden");formModeLabel.textContent="Manage Habits";formTitle.textContent="Add Habit";saveHabitBtn.textContent="Add Habit";cancelEditBtn.classList.add("hidden")}function startEditHabit(id){const h=loadHabits().find(x=>x.id===id);if(!h)return;editingHabitId=id;habitName.value=h.name;habitSchedule.value=h.schedule;habitBlock.value=h.block;habitCutoff.value=h.cutoff||"";habitAllowLate.checked=h.allowLate!==false;setSelectedCustomDays(Array.isArray(h.days)?h.days:[]);customDays.classList.toggle("hidden",h.schedule!=="custom");formModeLabel.textContent="Editing Habit";formTitle.textContent="Edit Habit";saveHabitBtn.textContent="Save Changes";cancelEditBtn.classList.remove("hidden");document.querySelector(".manage-card").scrollIntoView({behavior:"smooth",block:"start"})}function saveHabitFromForm(e){e.preventDefault();const name=habitName.value.trim(),schedule=habitSchedule.value;if(!name){alert("Add a habit name first.");return}if(schedule==="custom"&&selectedCustomDays.length===0){alert("Choose at least one custom day.");return}const habits=loadHabits(),data={name,schedule,days:schedule==="custom"?[...selectedCustomDays].sort():[],block:habitBlock.value,cutoff:habitCutoff.value,allowLate:habitAllowLate.checked};if(editingHabitId){const i=habits.findIndex(h=>h.id===editingHabitId);if(i>=0)habits[i]={...habits[i],...data}}else habits.push({id:makeId(name),...data});saveHabits(habits);resetFormMode();render()}function openSettings(){settingsPanel.classList.remove("hidden");document.body.style.overflow="hidden"}function closeSettings(){settingsPanel.classList.add("hidden");document.body.style.overflow=""}
function getExportPayload(){const habits=loadHabits(),completions=loadCompletions();return{version:3,exportedAt:new Date().toISOString(),habits,completions,stats:{overall:{currentStreak:getOverallCurrentStreak(),thisWeek:getWeekCount(),today:getDailyProgress(new Date())},byHabit:habits.map(getHabitStats)}}}function exportHabits(){backupBox.value=JSON.stringify(getExportPayload(),null,2);backupBox.focus();backupBox.select();if(navigator.clipboard)navigator.clipboard.writeText(backupBox.value).catch(()=>{});backupMessage.textContent="Backup created with habits, completion history, time blocks, and stats."}function importHabits(){const text=backupBox.value.trim();if(!text){backupMessage.textContent="Paste your backup first.";return}const parsed=safeParse(text,null),habits=Array.isArray(parsed)?parsed:parsed?.habits;if(!Array.isArray(habits)||habits.length===0){backupMessage.textContent="That backup does not look valid.";return}saveHabits(habits.map(normalizeHabit));if(parsed?.completions&&typeof parsed.completions==="object")saveCompletions(parsed.completions);resetFormMode();backupMessage.textContent="Data imported.";render()}
habitSchedule.addEventListener("change",()=>customDays.classList.toggle("hidden",habitSchedule.value!=="custom"));customDays.querySelectorAll("button").forEach(b=>b.addEventListener("click",()=>{const day=Number(b.dataset.day);selectedCustomDays=selectedCustomDays.includes(day)?selectedCustomDays.filter(d=>d!==day):[...selectedCustomDays,day];b.classList.toggle("selected",selectedCustomDays.includes(day))}));habitForm.addEventListener("submit",saveHabitFromForm);cancelEditBtn.addEventListener("click",resetFormMode);resetTodayBtn.addEventListener("click",resetToday);openSettingsBtn.addEventListener("click",openSettings);closeSettingsBtn.addEventListener("click",closeSettings);exportBtn.addEventListener("click",exportHabits);importBtn.addEventListener("click",importHabits);if("serviceWorker"in navigator)navigator.serviceWorker.register("service-worker.js");migrateOldDataOnce();formatDateLabel();render();